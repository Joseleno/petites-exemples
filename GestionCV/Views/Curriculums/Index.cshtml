@model IEnumerable<GestionCV.Models.Curriculum>

@{
    ViewData["Title"] = "CVs enregistrées";
}


<div class="formulaire">
    <h5>CVs enregistrées</h5>

    <p>
        <a asp-action="Create" class="btn btn-small orange darken-2">Nouveau CV</a>
    </p>
    <table class="table striped hoverable highlight">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nom)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nom)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.CurriculumId" class="btn-floating btn-small blue darken-4"><i class="material-icons">edit</i></a> |
                        <a asp-action="Details" asp-route-id="@item.CurriculumId" class="btn-floating btn-small deep-purple darken-4"><i class="material-icons">zoom_in</i></a> |
                        <a onclick="AfficherModal(@item.CurriculumId)" class="btn-floating btn-small red darken-4 modal-trigger"><i class="material-icons">delete_forever</i></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<div id="modal1" class="modal">
    <div class="modal-content">
        <h4>Confirmer la suppression</h4>
        <p>Voulez-vous confirmer la suppression?</p>
    </div>
    <div class="modal-footer">
        <a class="modal-close waves-effect btn orange accent-4">Pas maintenant</a>
        <a class="modal-close waves-effect btn red accent-4 btnOui">Oui</a>
    </div>
</div>

@section Scripts{
    <script>
        $(".modal").modal();

        function AfficherModal(id) {
            $("#modal1").modal("open");

            $(".btnOui").on('click', function () {
                $.ajax({
                    method: "POST",
                    url: "/Curriculums/Delete",
                    data: { id: id },
                    success: function () {
                        alert("CV supprimé avec succès");
                        location.reload();
                        $(".modal").modal('close');
                    }

                });
            })
        }
    </script>
}
